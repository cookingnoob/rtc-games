import React from 'react'

const Sodoku = () => {
    //tenemos un array de arrays con un array de valores
        //el array principal es el tablero
        //el array dentro el array crea la parrilla interna
        //el array dentro del array dentro del array son los numeros
    //Sistema igual al de gato para poner valores en la celda
    //sistema que checa si ganaste o perdiste

  return (
    <>

    </>
  )
}

export default Sodoku